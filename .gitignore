# Dependencies
node_modules/

# Build output (set once a generator is chosen, e.g. Astro `dist/`, Hugo `public/`)
dist/
public/
.astro/

# Env & secrets
.env
.env.*
!.env.example

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Logs
*.log
npm-debug.log*

# frontend-design ships `license: LICENSE.txt` we don't vendor — keep it local, don't
# redistribute it in this public repo. Other skills (with their own notices) ARE tracked.
.claude/skills/frontend-design/
